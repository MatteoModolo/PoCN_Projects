The list of used prompts is not fully complete and also doesn't correspond to the final programs as a lot modification,changes and a lot of different ways to coompute the analysis has been tested, so the following prompts are 
mostly possible prompts to submit to an agent in order to have reproducibility and are a oversemplification of the thought process behind. Also this prompts relate only to the final programs/analysis and plots proposed with all
initial ideas and intermediate fixing and changing missing. i don't dispose of the full list of prompts and configuration so i can't stress enough that is a very oversemplification of a work that took weeks.

comparisonModARI.R: Can you make me a R script  using dplyr and  ggplot2 that compares the outputs of three community detection models over time for Louvain, Leiden, and a stochastic block model based on Minimum Description lenght. The script needs to read CSV files containing the assignments from their respective directories (louvain_communities, leiden_communities, and sbm_mdl_blocks). First  calculate the total number of unique communities or blocks detected by each of the three models. Second calculate the Adjusted Rand Index (ARI) to compute the structural divergence  of the Louvain and SBM models using the Leiden partition as the baseline. Finally pltot the results and resize the y axis to avoid letting the scale being influenced by anomalous data
compTurn.R:Can you write an R script using readr, dplyr,ggplot2, and tidyr to perform a monthly on BGP network graphs The script should read through a directory called final_weighted_networks containing gzipped TSV edge lists (pattern: ^as_edges_.*\\.tsv\\.gz), where the columns are asn1 and asn2.
Loop through the files consecutively and carry over the second month's data into the next iteration so we don't read the same file twice. For each pair of consecutive months, extract the unique nodes and create a fast string ID for every edge. Then calculate the number of persistent, appearing, and disappearing nodes and edges. Also, calculate the similarity index for both nodes and edges. Save all of these metrics into a dataframe and export it as network_turnover_analysis.csv.
create a visualization for the edge turnover using ggplot2. 
connectedComponents.R:Can you write an R script using dplyr,igraph and ggplot2 to extract data from data folder (specified) andthe year and month from the filename and then build the igraph objects efficiently extracting all unique ASNs  convert them to integer IDs, and feed that integer matrix into graph_from_edgelist to crewate an undirected network. Once built extract the sizes of all connected components. I want to track the exact sizes of the top 100 components for every month. Extract the top 100 sizes and put to 0 the NA values Save this main table as top_100_components_monthly.csv with columns for the month, total nodes, and comp_1 through comp_100.
Inside the loop if a network is not perfectly connected  log it into a separate fragmented networks list. Record the month, total nodes, LCC size, number of disconnected nodes, total components, and the size of the second largest component. save fragmented_networks_log.csv if any fragmented networks were actually found.
Calculate the ratio of the LCC to the total nodes . Plot this ratio using a ggplot2 line. zoom the y-axis tightly between 0.98 and 1, and use the scales package to format the y-axis. To keep the x-axis from getting messy, extract just the very first and last months and set them as the only ticks. U
The script should read gzipped TSV edge lists from a final_weighted_networks directory (pattern: ^as_edges_.*_weighted\\.tsv\\.gz$).
degDist.R: Can you write an R script using dplyr and igraph to extract the degree distributions from a series of BGP networks? The script should read edge lists from a final_weighted_networks directory (pattern: ^as_edges_.*_weighted\\.tsv\\.gz$) and save the results into a new directory called degree_distribution
For the processing loop, extract the year and month from the filename. if the output CSV for a specific month already exists skip it to save processing time.
extract all unique ASNs  convert them to integer IDs and feed it into graph_from_edgelist(). Once the graph is built, calculate the full degree distribution with the funcction degree_distribution(g). Map these probabilities into a dataframe with two columns degree and probability.
filter out any degrees that have a probability of exactly 0. Write this cleaned dataframe to its respective CSV
DoubleCheckIntegrity.R:Can you write an R script to build to cross-reference the files I have saved locally against the official RIPE repository to see what is missing, broken, or ready for analysis.
Check the network files present at the URL https://data.ris.ripe.net/rrc00/](https://data.ris.ripe.net/rrc00/. Extract all the href links and check which files are present in final_weighted_networks directory (pattern: as_edges_YYYY_MM_weighted.tsv.gz).If the file doesn't exist locally at all, flag its status as Corrupted/Missing.
If it loads successfully, extract the total number of unique nodes. If the node count is less than a  threshold of 10, flag it as 'MissingData' (as a network that tiny implies collector failure).If it passes all these checks, flag it as AnalysisReady.
Print the month and status to the console as it runs, and once the loop is finished, bind all the results into a dataframe and save it as network_status_log.csv
fitassort.R:Can you write an R script using dplyr and ggplot2 to evaluate the  network assortativity  reading a dataset called topological_correlations.csv.
 calculate a continuous numeric time variable representing the exact number of days since the first snapshot in the dataset. exlude the timeframe from 2020 to 2022.
Do a maximum likelihood estimation fit on the temporal trend. Under gaussian error assumptions, simply use lm() to regress the assortativity exponent against the continuous numeric time variable, Extract the slope, the exact 95% confidence interval , the p-value Add a check if the slope is negative and the p-value is < 0.05
create a ggplot2 visualization. Overlay the regression line along with its 95% CI shaded ribbon in red Draw a dashed black horizontal line at y = 0 to visually separate assortative (top) from disassortative (bottom) networks. save the plot as mu_disassortative_trend_mle.png
fragile2.R: Can you write an R script using readr, dplyr, igraph, and ggplot2 to simulate and visualize the evolution of network percolation, the script should load empirical parameters from ultimate_distribution_confrontation.csv. filter the data to find the very first available month for the years 2002, 2016, and 2024.Next, build a custom network generator function for N=100000 nodes with multiple realization for each network type. It must generate four types of networks based on the empirical parameters. for the powerlaw, exponential lognormal extract the results of the fit from the file. Finally, for the ER baseline simulate networks with the same average degree as the one in the powerlaw simulation.Create a percolation simulator that tracks the normalized size of the LCC as the occupancy probability drops from 1 to 0. It should handle both 'random' (shuffled) and 'targeted' (sorted descending by degree) attacks using .For the main simulation loop, iterate through the three target months and run 50 realizations each.  Run both attack types on all four networks and store the results.Finally, aggregate the mean LCCs and generate two ggplot2 charts one for random failures and one for targeted Attacks
NodeEdgeExtr.R: can you write an R script using readr, dplyr, stringr, ggplot2, and patchwork to extract and visualize the number of nodes and edges of a BGP network over time?For the extraction loop, extract the year and month from the filename. load only the first to column. If the file loads successfully, calculate the total number of edges (the number of rows) and the total number of unique nodes (unique elements across both columns). Append these to a dataframe, sort it cronologically by month at the end, and save it as network_metrics_summary.csv.Finally generate a visualization using patchwork to show the network's evolution of nodes and edges
 The script should scan a directory called final_weighted_networks for gzipped TSV files matching the pattern ^as_edges_.*_weighted\\.tsv\\.gz$
lifespantest.R: Can you write an R script using readr, dplyr, stringr, and ggplot2 to track the lifespans of the highest degree nodes in a BGP network and visualize them as a waterfall chart? The script should scan the final_weighted_networks directory for gzipped TSV edge lists.
calculate the exact lifespan of every node that ever entered that has been in the top 100 highest degree nodes in any month. Group by node and calculate its first seen month, last seen month, and total months present. arrange the dataframe by status, start date, and descending total months and plot
tryTopNodes.R: Can you write an R script using dplyr, ggplot2 to visualize the permanance of the highest degree nodes nodes in a BGP network? The script should process a dataset called top_30_nodes_monthly.csv.
Isolate the exclusive list of unique nodes that achieved Rank 1 in at least one month.  For those specific Rank 1 nodes only, filter the dataset to count how many total months they managed to stay inside the Top 10. Group by node, summarize this count, and calculate their persistence fraction by dividing this Top 10 count by the total months in the dataset. Sort the resulting dataframe from highest fraction to lowest. Finally, visualize this as a  bar chart using geom_col . Reorder the x-axis so the bars sort descending by their fraction. 
leidenCD.R:Can you write an R script u to perform Leiden community detection across a dataset of BGP networks? The script should read gzipped TSV edge lists from final_weighted_networks and save the outputs to a new dedicated directory called leiden_communities.
Inside the processing loop, build the undirected graph using graph_from_data_frame. Use the function cluster_leiden(), explicitly setting the objective_function = "modularity"to be comparable to the standard Louvain method. Extract the global modularity score, total community count, nodes, and edges, and append them to a summary list. Furthermore, extract the exact community assignment for every single node and save it as a monthly footprint CSV 
Make a visualization of the commuinities for the month 2016_01. To accurately and visually compare this Leiden clustering against a previous Louvain run, the nodes must occupy the exact same spatial coordinates. look for a pre-saved layout_YYYY_MM.rds file inside a strictly defined louvain_communities directory.  load it as the graph_layout. bind the global metrics into a dataframe and save it as leiden_modularity_summary.csv
mixture.R: Can you write an R script to evaluate the degree correlation  of BGP networks over time with nearest neighbor statistics? The script should scan final_weighted_networks for gzipped TSV edge lists.First calulate the average degree of the neighbor for each node then g group the dataset by degree and calculate the global average for each degree class, filtering out any zeros.For each month, fit a power-law relationship to these degree classes using log-log linear regression Extract the scaling exponent mu. .Generate an individual scatter plot for each month showing the knn distribution. Use logscale. Plot the data points and overlay the regression fit. Save these to a dynamically created directory called knn_correlation_plots.After looping through all months, save teh results topological_correlations.csv. Finally, generate a timeline plot of mu over time. Add a dashed black horizontal line precisely at y=0. Extract and use only the absolute first and last months for x axis
powerlaw.R: Can you write an R script  to fit a power-law distribution of BGP network degree data? The script should scan a degree_distribution directory for CSV files matching ^degree_dist_.*\\.csv$.To reconstruct the raw data, first load a secondary file called network_monthly_statistics.csv and extract the exact total number of nodes for that specific month. Multiply the probability column by this total node count and round it to get the exact occurrence counts. Filter out any 0 degrees before fitting. Use estimate_xmin() to dynamically find the optimal lower bound. Extract the 95% Confidence Intervals for both xmin and alpha using the quantile() function at 0.025 and 0.975 on the bootstrap results.save then results in a csv file and plot the results for each month with the fit overlapping the data
alternativeDistro.py: Can you write a Python script using the powerlaw library to perform the confrontation between the power-law distribution and a log normal distribution, exponential, powerlaw with exponential decay and stretched exponential on degree distribution of bgp networks? The script should process degree distribution CSVs located in a degree_distribution directory.load a file called network_monthly_statistics.csv. Evaluate the log-likelihood ratio and p-value between the power-law and four alternatives. Extract the specific parameters for each distribution , append them to a list, convert to a pandas DataFrame and save it as ultimate_distribution_confrontation.csv.Finally, create a plot tracking the four loglikelihood ratios over time.  Draw a dashed black horizontal line precisely at y=0 
SBMcommDet.py:"Can you write a Python script using graph_tool.all to perform Stochastic Block Model  inference on BGP networks using Minimum Description Length  optimization? The script should scan the final_weighted_networks directory for gzipped TSVs matching as_edges_*.tsv.gz.
 Run the MDL optimization by calling gt.minimize_blockmodel_dl(g), which will automatically find the optimal number of blocks by minimizing the description length. Extract the MDL score via state.entropy(), the total blocks via state.get_B(), and the total nodes. Print these to the console.
extract the exact community footprints. Retrieve the block property map using state.get_blocks(). Use a list comprehension over g.vertices() to map the original ASN to its assigned SBM block. Save this node-block mapping as a CSV (sbm_nodes_YYYY_MM.csv) into a dedicated sbm_mdl_blocks directory. the plot the resulsts
Finally, compile the total nodes, block counts, MDL entropy scores, and compute times for all months into a master dataframe and save it as sbm_mdl_summary.csv
